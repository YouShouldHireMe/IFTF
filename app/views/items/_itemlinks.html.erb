<div class="page_title">
    All items linked to <%= @item.title %>
</div>
<ul class="styled_list">
    <% @items.each do |item| %>
        <li>
            <%= link_to item.title, item_path(item), :remote=>true %>
        </li>
    <% end %>
</ul>
