$('#list_items').append('<%= j render partial: "listitem", collection: @items, as: :item %>');
<% if @items.last_page? %>
    $('#resource_list').off('scroll');
    $('#pagination').hide();
<% else %>
    $('#pagination').html('<%= j (paginate(@items, :remote => true).to_s) %>');
<% end %>

